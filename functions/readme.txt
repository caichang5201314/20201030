1、为什么要有函数？
     业务代码的复用
  
2、函数定义
def 函数名([参数]):
	业务代码
	[return ..]
	
3、调用
	直接写函数名即可,如果跨包或跨文件，只需要引入即可(ctrl+shift+o)
	
4、参数
	1、你首先要明确，为什么要用参数，不用不行吗？
		我先写的是无参的函数，业务直接被包裹
		但是，我觉得业务中有有的东西有点“死”，我想写灵活一点
		
		
		
		
		